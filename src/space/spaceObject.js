export class SpaceObject {
    constructor({radius, color, name, x, y}) {
        this.radius = radius;
        this.color = color;
        this.name = name;
        this.x = x;
        this.y = y;
    }
}